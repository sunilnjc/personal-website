import {notFound} from 'next/navigation';
import {caseStudies} from '../../case-studies';
export default async function Project({params}:{params:Promise<{slug:string}>}) {
 const {slug}=await params; const project=caseStudies.find(p=>p.id===slug); if(!project)notFound();
 return <><header className="site-header"><a className="wordmark" href="/">sunil<span>●</span></a><a href="/#projects">← All projects</a></header><main className="wrap case-page"><p className="eyebrow">{project.category} / {project.status}</p><h1>{project.title}</h1><p className="case-lead">{project.summary}</p><div className="panel-links"><a className="action action-primary" href={project.url} target="_blank" rel="noopener noreferrer">Open live application ↗</a>{project.repo&&<a className="action" href={project.repo} target="_blank" rel="noopener noreferrer">View source ↗</a>}</div><div className="case-columns"><section><h2>The problem</h2><p>{project.problem}</p></section><section><h2>What I built</h2><p>{project.contribution}</p></section></div><section className="case-section"><h2>Architecture at a glance</h2><p className="subtle">A simplified view of the main application components.</p><ol className="architecture">{project.architecture.map(([label,text],i)=><li key={label}><span className="step-number">0{i+1}</span><h3>{label}</h3><p>{text}</p></li>)}</ol></section><section className="case-section"><h2>Engineering decisions</h2><div className="decisions">{project.decisions.map(([title,text])=><article key={title}><h3>{title}</h3><p>{text}</p></article>)}</div></section><section className="case-section"><h2>Technology</h2><div className="tags">{project.stack.map(t=><span key={t}>{t}</span>)}</div></section><div className="case-columns"><section><h2>Current outcome</h2><p>{project.result}</p></section><section><h2>What I would improve next</h2><p>{project.next}</p></section></div><a className="action" href="/#projects">← Back to portfolio</a></main></>;
}
